// @change/motion-tokens — fuente única del sistema de Motion del Change DS.
// CSS: import "@change/motion-tokens/css"  → expone las custom properties.
// JS:  import { transition, revealVariants, stagger, EASE, DURATION_S } from "@change/motion-tokens"
export { DURATION_MS, DURATION_S, EASE, STAGGER_S } from './tokens'
export { transition, revealVariants, stagger } from './presets'
